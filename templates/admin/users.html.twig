{% extends 'base.html.twig' %}

{% block title %}Liste des utilisateurs{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <!-- Barre de recherche -->
    <div class="mb-4">
        <form method="get" action="{{ path('admin_users') }}" class="flex">
            <input type="text" name="search" placeholder="Rechercher des utilisateurs..." 
                class="flex-grow p-2 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-orange-400" 
                value="{{ app.request.get('search')|default('') }}">
            <button type="submit" 
                class="ml-2 px-4 py-2 bg-orange-600 text-white rounded-lg hover:bg-orange-700 focus:outline-none focus:ring-2 focus:ring-orange-500">
                Recherche
            </button>
        </form>
    </div>

    <!-- Détails de l'utilisateur sélectionné -->
    {% if selectedUser %}
        <div class="mb-4 bg-white p-4 rounded-lg">
            <div class="flex justify-between items-center mb-4">
                <h1 class="text-2xl font-bold text-orange-600">{{ selectedUser.email }}</h1>
                <div class="space-x-2">
                    <a class="text-orange-600 hover:text-orange-800">✏️</a>
                    <a class="text-orange-600 hover:text-orange-800">🗑️</a>
                </div>
            </div>
            <div class="w-full gap-2">
                <div class="flex justify-between items-center">
                    <p class="font-bold">Prénom</p>
                    <p>{{ selectedUser.firstname }}</p>
                </div>
                <div class="flex justify-between items-center">
                    <p class="font-bold">Nom de famille</p>
                    <p>{{ selectedUser.lastname }}</p>
                </div>
                <div class="flex justify-between items-center">
                    <p class="font-bold">Nombre de réservations</p>
                    <p>{{ selectedUser.getReservations|length }}</p>
                </div>
            </div>
        </div>
    {% endif %}

    <!-- Tableau des utilisateurs -->
    <table class="min-w-full bg-white border border-gray-300 rounded-lg shadow-md">
        <thead>
            <tr class="bg-orange-600 text-white">
                <th class="px-4 py-2 text-left">Prénom</th>
                <th class="px-4 py-2 text-left">Nom</th>
                <th class="px-4 py-2 text-left">Email</th>
            </tr>
        </thead>
        <tbody>
            {% for user in users %}
                <tr class="border-b border-gray-300 cursor-pointer {% if selectedUser and selectedUser.id == user.id %}bg-orange-100{% endif %}" onclick="window.location.href='{{ path('admin_users', { selected: user.id }) }}'">
                    <td class="px-4 py-2">{{ user.firstname }}</td>
                    <td class="px-4 py-2">{{ user.lastname }}</td>
                    <td class="px-4 py-2">{{ user.email }}</td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="5" class="px-4 py-2 text-center text-gray-500">Aucun utilisateur trouvé</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
